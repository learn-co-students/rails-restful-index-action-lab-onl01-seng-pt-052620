
<!DOCTYPE html>
<html>

<div>
  <% @students.each do |student| %>
    <div><%= student.to_s %></div>
  <% end %>
</div>

<%# content_tag(:div, @students, class: "student-index-page") do %>
  <%# content_tag(:p, "#{@student.first_name}: #{@student.last_name}") %>
<%# end %>